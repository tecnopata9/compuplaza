document.addEventListener('DOMContentLoaded', () => {
    // Inicializar AOS (Animaciones)
    AOS.init();

    // Inicializar Lucide (Iconos)
    lucide.createIcons();
});

// Inicializar Alpine (Interactividad)
// Nota: Alpine suele auto-iniciarse si usas la versión CDN/dist
